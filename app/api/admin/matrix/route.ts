import { NextRequest, NextResponse } from "next/server"; import { saveMatrixRule } from "../../../../lib/server-control";
export const runtime="nodejs"; export async function POST(req:NextRequest){try{const actor=req.headers.get("x-nexus-user")||""; return NextResponse.json({ok:true,rule:await saveMatrixRule(actor,await req.json())});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Error"},{status:403});}}
