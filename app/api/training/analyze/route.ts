import { NextRequest, NextResponse } from "next/server";
import { submitAgent, view } from "../../../../lib/server-queue";
export const runtime="nodejs"; export const dynamic="force-dynamic";
export async function POST(request:NextRequest){const body=await request.json();const caseText=String(body.caseText||"").trim();if(!caseText)return NextResponse.json({error:"caseText is required"},{status:400});const trainer=String(body.trainer||"training").trim();const sessionId=String(body.sessionId||`training-${Date.now()}`);const prompt=["TRAINING REVIEW — HOTELPLANNER ZENDESK OPERATIONS","Analyze the case using the authoritative Ticket Matrix when relevant.","Do not invent requirements that are not explicitly supported by the Matrix.","Return: concern, matched Matrix rule(s), required next actions, escalation/ticket/refund/VIPRES requirements when applicable, and a concise internal-note suggestion.","Clearly distinguish official Matrix requirements from learned memory or inference.","CASE:",caseText].join("\n\n");try{return NextResponse.json(await submitAgent(trainer,{prompt,session_id:sessionId,user_id:trainer,mode:"qa",engine:"nexus",allow_tools:false,allow_learning:false}),{status:202});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Nexus unavailable"},{status:503});}}
export async function GET(request:NextRequest){const id=request.nextUrl.searchParams.get("job_id");if(!id)return NextResponse.json({error:"job_id is required"},{status:400});const job=view(id);return job?NextResponse.json(job):NextResponse.json({error:"Job not found"},{status:404});}
